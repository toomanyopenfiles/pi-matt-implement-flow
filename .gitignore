# 开发过程学习记录，不进公开仓库
docs/learning/

# matt 流程运行期产物（D14：不进 git status，避免拦住 worktree 派发）
.scratch/

# 编排器账本 / review bundle / findings
.pi/matt-implement/

# pi-subagents 运行期状态（pi 自身已排除，这里让我们的自检口径一致）
.pi/subagents/

# 依赖与构建产物
node_modules/
