# NOTES

教学偏好与工作记录（给未来的自己看）。

## 用户偏好

- 语言：简体中文回复；专有名词适当加括注，避免过多中英混杂
- 工作区选定于 `docs/learning/`，**随包发布**——写课程时注意内容质量（会进 git，别人可能看到）
- 会话开在 brainstorming 模式：不做项目功能变更；教学文件的创建在用户选定方案后即视为授权

## 教学状态记录

- 2026-09-15（第 1 次会话）：先讲了一遍 SKILL.md 完整执行流程（纯讲解），随后激活 teach 技能，选定使命「维护并演进这个插件」。
- 课程 0001 已交付并通过：排序 2 次全对 + quiz 4/4（见 learning-records/0001）。用户主动追问「锚定+三点 diff」和「--no-ff」——提问落在具体 git 语义而非流程，讲解时可假设流程骨架已立，git 底层概念要配具象图示。
- 新术语答疑时顺带补进速查页（用户对 reference 的依赖度待观察）。
- 课程 0002（worktree 与 resume）已交付。
- **链接纪律（被用户抓到 0001 链接全坏后立的规矩）**：课程/参考页写完后，一律用解析脚本逐 href 审计；仓库外文档（pi 平台 docs、pi-subagents 源码）不用相对链接，统一指向仓库内 verified-facts 的转录。
- **结构纪律（0002 v1 反馈后确立，LR-0003）**：先给「执行者 | 目标 | 输入→输出 | 归属」步骤表 + 角色着色流程图，再谈机制；设计史/弯路一律 `<details>` 折叠，不占主线。用户 git 不熟：命令首次出现配一句人话，跨课复用 reference/git-minimal.html。
- 全部源码已通读一遍（SKILL.md、3 个 agent、4 份设计文档、3 份 docs/agents、registration-checks.js）。还没读：self-check.test.js、git-version-check.js——后续讲自检套件的课再读。

## 出题纪律

- quiz 各选项字符数尽量相等（中文按字符数），不给格式线索
- 练习设计要区分「知识获取」（低难度）与「技能巩固」（检索练习、间隔、混插）
