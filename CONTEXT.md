# CONTEXT — pi-matt-implement-flow

本包的域词汇表。编排器（orchestrator）的记忆层由三层构成，写权分层是本域的核心约束；
下列词条即协议词汇，输出一律按此用词，不使用同义词漂移。

## Glossary

### 台账（ledger）

派生显示层：人类可读的 markdown 运行状态视图，四段结构——头部（含 `state: running|complete`）、
票表格、事件渲染时间线、对账结论。台账由事件流与真相层**全量再生**，**脚本独占写权**：
LLM 与人类都不手写、不手改；表格漂移或文件损坏时靠重跑脚本确定性重建，而不是争论。

### 事件流（event stream）

append-only 的 JSONL 机器事实文件，一行一个结构化事件；每行由脚本盖上权威时间戳、
单调序号、格式版本字段与写入时刻的 git HEAD 锚点。事件流是 LLM 的**唯一状态写面**——
编排器通过记账命令写入事件，此外任何对事件流的直接修改（shell 追加、edit 工具）都被禁止。

### 编排笔记（orchestration notes）

散文记忆：过程叙事、教训、用户口头拍板等**非结构化**内容的固定去处。
编排笔记独立于结构化状态存在，永不与事件流竞争真相源地位；
凡是"发生了什么、何时"级别的事实归事件流，凡是"为什么、学到什么"级别的散文归编排笔记。

### 记账（record an event）

通过脚本的 `add` 命令向事件流写入一条事件：脚本先校验载荷（拒绝时给出原因，供立即修正重试），
通过后盖时间戳与序号、append 到事件流，并**自动再生台账**。
记账是编排器记录每个状态转换（派发、结算、评审裁决、修复、合并、升级、封账……）的唯一动作；
"更新台账"不是一个独立步骤，它随记账自动发生。

### 封账（seal the ledger）

记账 `close` 事件：台账头部 `state` 置为 `complete`，运行终结。
封账是显式的机械标志——下一个会话一眼可知运行已结束，失真的"进行中"表格不可能再冒充活跃运行；
封账之后记账命令拒绝一切新事件。
封账门：有合并工作的运行须已有终审裁决入账（`final` 事件）方可封账；最新裁决为
`ready` / `ready_with_fixes` 视为已过终审；为 `not_ready` 时封账仍放行但台账标注警告——
那是用户拍板放弃的合法出口。

### 对账（reconcile）

通过脚本的 `check` 命令核验账实差异：把事件流 + 真相层（git、worktree 列表、票文件 Status、
gh PR 状态 best-effort）与台账记录对照，发现漂移则逐条列出并以非零退出码报告。
对账门在每次派发与合并前执行；compaction 恢复时与台账再生（`build`）一同执行。

### 流程形态（flow shape）

一次 run 的流程开关组合：是否逐票评审、每票修复预算、并发 coder 数。来源是 settings
顶层自定义节 `mattImplementFlow`（本包私有，不碰平台键），在记账 `init` 时以旗标**冻结**
进事件流；此后 ledger 校验一律按快照执行——中途改配置不影响进行中的 run。
旧账本无旗标时按默认形态（评审开 / 预算 2 / 并发 3）解释。

### 终审（final review）

全部票闭环后对整条 feature 分支的一次整体评审：专看单票评审看不见的跨票问题——跨文件漂移、
组件互相矛盾、spec 有要求却无票承接、文档与实现失配。终审是固定环节，不受流程形态
逐票评审开关影响（`reviewer=off` 的运行终审照跑）。结论为三值裁决：`ready`（就绪）/
`ready_with_fixes`（就绪、附带待修问题）/ `not_ready`（未就绪）；多轮终审（裁决 → 修复 → 重审）
按入账顺序排列，一律以最新裁决为准。裁决属状态转换级事实，记账 `final` 事件入事件流；
派发终审这个动作本身不记事件（与票级 reviewer 派发同一先例，runId 由裁决事件承载）。

### tracker

spec 与工单的落点系统，形态由 `docs/agents/issue-tracker.md` 定义：local markdown
（`.scratch/<feature>/`）或 GitHub Issues。票号一律采用 tracker 原生编号（local 为文件序号
补零两位，GitHub 为 issue number）；定位一次 run 的 spec 的标识是 spec 引用——local 传
spec 文件路径，GitHub 传 issue 号/URL。

### tracker 快照（tracker snapshot）

run 初始化时把 tracker 上的 spec 与全部工单拉取到运行时目录
（`.pi/matt-implement/<slug>/tracker/`）的工作副本，与 local tracker 票文件同构
（含 `Status:` / `Type:` / `Blocked by:` 行）。快照是本地全链路（派发、评审、对账、记账）
的唯一读写对象，是**待推送的真相**；tracker 本体是它的延迟镜像，不再是真相层——
对账所核验的"票文件 Status"即快照票文件。与流程形态里的"旗标快照"无关。

### 同步（sync）

封账前把 tracker 快照的状态差异批量推送到 tracker 本体的单点动作：合并的票关票并附
merge SHA、升级的票留评保持开放、spec 收尾关闭。同步必须发生在封账**之前**（封账后
事件流拒写，同步失败将无从记账），并设计为幂等可重入。

### 占坑（claim the spec）

以在 tracker 上认领 spec 为形式的并发锁，声明某个 feature 已被本 run 占用，防止多
会话/多人同时开跑。占坑是 run 的第一个写动作，先于快照拉取；锁语义实时生效，与
进度同步分离（同步延迟不影响锁）。封账同步或放弃时释放。

### 认领（claim a ticket）

把单张工单标记为在途的进度信号：在 tracker 快照上写 `Status: claimed`。认领不是锁
（feature 级互斥由占坑负责），也不上 tracker 本体——tracker 侧的进度痕迹只有同步时
的关票评论。

### 续跑（run continuation）

以同一命令重启一次未封账的 run：检测到既有事件流即跳过初始化与拉取，经台账再生
（`build`）与对账（`check`）重建状态后从前沿继续。与子代理的上下文续用（resume）
无关——那是平台机制，不是 run 级动作。

### 传输件（transfer artifact）

为只读子代理准备的运行时输入文件（review bundle、findings、tracker 快照），性质是
"递给不能读网络的审查者的快照"：不是文档，也不是真相源——不入 git、不进检索面、
用后即弃。运行时文件三分：机器状态（账本三件套，长存）、传输件（用后即弃）、
tracker 快照（同步成功后清理）。

## 退役词

### ~~事件日志~~（retired）

退役。旧协议中一词两义：既指手工维护的 append-only 散文日志，又指机器事件流——
歧义正是上次重构要消灭的对象。现协议中：散文去处 = **编排笔记**；机器事实 = **事件流**。
读到旧文档中的"事件日志"时，按上下文映射到这两个词条之一，不再产出该词。
