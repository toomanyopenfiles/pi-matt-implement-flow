{
  "name": "pi-matt-implement-flow",
  "version": "0.1.0",
  "description": "pi 的 implement 阶段编排器：读 spec + 票 → 算前沿 → 并行派 coder（各自 worktree）→ 两轴 review → 接回同一 coder 修复 → 合并 + 集成测试门 → 重算前沿 → 最终双轴 review",
  "keywords": ["pi-package"],
  "scripts": {
    "test": "node --test scripts/*.test.js"
  },
  "license": "MIT",
  "pi": {
    "skills": ["./SKILL.md"],
    "subagents": {
      "agents": ["./agents"]
    }
  }
}
