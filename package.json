{
  "name": "pi-matt-implement-flow",
  "version": "0.1.0",
  "description": "Orchestrated parallel implementation for pi: coders work a spec's ticket graph in isolated worktrees, every ticket passes a two-axis review with fixes looped back, merges face a full test gate, and the branch finishes with a whole-branch review.",
  "keywords": ["pi-package"],
  "scripts": {
    "test": "node --test scripts/*.test.js"
  },
  "license": "MIT",
  "pi": {
    "skills": ["./SKILL.md"],
    "extensions": ["./extensions"],
    "subagents": {
      "agents": ["./agents"]
    }
  }
}
